include:
  - docker-compose.yml

services:
  redirect-service:
    build: .
    ports:
      - "8080:8080"
    environment:
      DATABASE_URL: postgres://app:app@postgres:5432/shortener
    depends_on:
      postgres:
        condition: service_healthy